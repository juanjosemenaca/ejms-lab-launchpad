import { ContactSubmissionsPanel } from "@/components/admin/ContactSubmissionsPanel";

/** Mensajes del formulario de contacto público; enlace en el hub «Mensajes» del menú lateral. */
const AdminWebFormMessages = () => (
  <div className="max-w-6xl">
    <ContactSubmissionsPanel />
  </div>
);

export default AdminWebFormMessages;
